# Environment and secrets
.env
.env.*
# ...except the template, which holds no values and is the documentation for
# every variable the server reads. `.env.*` above would otherwise swallow it.
!.env.example
*.pem
tokens.json

# Claude Code context and settings
.claude/

# Dependencies
node_modules/

# Database and runtime state
temp_backups/

# IDE
.idea/
.vscode/

# OS files
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/

# Testing and coverage
coverage/
.nyc_output/

# Build outputs
dist/
build/
*.tsbuildinfo

# Design handoff from Claude Design: working reference only, never committed.
# Deleted entirely as an exit criterion of the final P1-WP9 tranche.
design_handoff_bot_desktop_app/
